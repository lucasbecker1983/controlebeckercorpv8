#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="{{ config.stack.project_root }}"

cd "${PROJECT_ROOT}/backend"
npm install
npm run build

cd "${PROJECT_ROOT}/backend-proxy"
npm install
npm run build

cd "${PROJECT_ROOT}/frontend"
npm install
npm run build

if command -v pm2 >/dev/null 2>&1; then
  pm2 startOrReload /etc/sgcg/installer/generated/ecosystem.config.cjs --update-env
  pm2 save
fi

echo "Deploy base do SGCG concluido"
